from tkinter import *
from sqlite3 import *
import db_fonk
import csv

connectDatabase = connect("cms.db")
db_cursor = connectDatabase.cursor()
root=Tk()
menubar = Menu(root)

class course:

    def __init__(self,courseName="Empty",courseCode="Empty",courseId="Empty",courseBook="Empty",refBookName="Empty",syllabus="Empty"):
        self.courseName = courseName
        self.courseCode = courseCode
        self.courseId   = None
        self.courseBook = courseBook
        self.refBookName= refBookName
        self.syllabus=    syllabus
 
def insertCourseInDatabase(self,courseCode,courseId):

    def setCourseName(self, name):
        self.courseName = name

    def setCourseCode(self,code):
        self.courseCode = code

    def setCourseId(self,courseId):
        self.courseId=courseId

    def setCourseBook(self,name):

    def setRefBookName(self,name):

    def getCourseName(self,name):
        return self.courseName

    def getCourseCode(self,code):
        return self.coursCode

    def getCourseId(self,code):
        return self.semesterCode

    def getCourseBook(self,name):
        return self.sectionCode

    def getRefBookName(self,Name):
        return self.refBookName

    def getsyllabus(self):
        return self.syllabus

    def insertCourseInDatabase(self,courseCode,courseId):
        self.courseCode = db_fonk.insert_Course(self.courseName, self.courseCode, self.courseId,self.courseBook,self.refBookName,self.syllabus)
        db_fonk.insert_Coursein(courseCode, courseName, self.courseId)
        db_fonk.save_changes()

    def displayCourse(self):
        print("Course Name:",self.getCourseName())
        print("Course Code:", self.getCourseCode())
        print("Course Id:",self.getCourseId())
        print("Course Book:",self.getCourseBook())
        print("RefBook:", self.getRefBookName())
        print("Syllabus:",self.getsyllabus())
        
        
        
